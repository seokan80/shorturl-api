spring:
  profiles:
    active: local
  application:
    name: short-url-admin
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy

jasypt:
  encryptor:
    password: ${JASYPT_ENCRYPTOR_PASSWORD}
    algorithm: "PBEWITHHMACSHA512ANDAES_256"
    iv-generator-classname: org.jasypt.iv.RandomIvGenerator
    pool-size: 1

short-url:
  # 단축 URL 생성 시 기본 만료 기간(일). 재빌드 없이 운영 중 조정 가능.
  default-expiration-days: 1
  # redirect 서버가 단축 URL 생성 후 공개 주소를 조합할 때 사용하는 prefix
  redirect:
    public-url: http://localhost:8080/r/
  # redirect 서버가 /api/internal/redirection-config 로 폴링해 가져가는 설정
  redirection:
    fallback-url: "https://www.nhbank.com"
    default-host: "https://www.nhbank.com"
    show-error-page: true
    tracking-fields: "utm_source,utm_medium,utm_campaign"
