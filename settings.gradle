pluginManagement {
	repositories {
		maven { url = 'https://repo.spring.io/snapshot' }
		gradlePluginPortal()
	}
}
rootProject.name = 'short-url'

include 'common'
include 'short-url-admin'
include 'short-url-redirect'
include 'short-url-admin-ui'
